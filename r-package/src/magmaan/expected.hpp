// @generated by dev/vendor-cpp.sh from include/magmaan/expected.hpp — DO NOT EDIT; re-run `just vendor`.
#pragma once

#include <expected>

#include "magmaan/error.hpp"

namespace magmaan {

template <class T>
using parse_expected = std::expected<T, ParseError>;

template <class T>
using partable_expected = std::expected<T, PartableError>;

template <class T>
using model_expected = std::expected<T, ModelError>;

template <class T>
using fit_expected = std::expected<T, FitError>;

template <class T>
using post_expected = std::expected<T, PostError>;

template <class T>
using sim_expected = std::expected<T, SimError>;

}
