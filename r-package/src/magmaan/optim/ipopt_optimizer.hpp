// @generated by dev/vendor-cpp.sh from include/magmaan/optim/ipopt_optimizer.hpp — DO NOT EDIT; re-run `just vendor`.
#pragma once

#ifndef MAGMAAN_WITH_IPOPT
// Stub-only header when IPOPT is disabled. This mirrors the Ceres header shape:
// callers can include it unconditionally, but declarations are available only
// in IPOPT-enabled builds.
namespace magmaan::optim {
struct IpoptOptimizer;
}  // namespace magmaan::optim
#else

#include <functional>
#include <string_view>

#include <Eigen/Core>

#include "magmaan/expected.hpp"
#include "magmaan/optim/problem.hpp"

namespace magmaan::optim {

// IPOPT interior-point optimizer. v1 uses IPOPT's limited-memory Hessian
// approximation, so callers need only objective gradients and constraint
// Jacobians.
class IpoptOptimizer {
 public:
  static constexpr std::string_view name = "ipopt";

  IpoptOptimizer(OptimOptions opts = {}) noexcept : opts_(opts) {}

  OptimOptions options() const noexcept { return opts_; }

  using Objective = ObjectiveFn;

  fit_expected<OptimOutput>
  minimize(Objective f, const Eigen::VectorXd& x0) const;

  fit_expected<OptimOutput>
  minimize(Objective f, const Eigen::VectorXd& x0,
           const Eigen::VectorXd& lower,
           const Eigen::VectorXd& upper) const;

  fit_expected<OptimOutput>
  minimize_constrained(const ConstrainedScalarProblem& prob,
                       const Eigen::VectorXd& x0,
                       const Eigen::VectorXd& lower,
                       const Eigen::VectorXd& upper) const;

 private:
  OptimOptions opts_;
};

}  // namespace magmaan::optim

#endif  // MAGMAAN_WITH_IPOPT
