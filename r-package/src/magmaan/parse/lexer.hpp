// @generated by dev/vendor-cpp.sh from include/magmaan/parse/lexer.hpp — DO NOT EDIT; re-run `just vendor`.
#pragma once

#include <cstdint>
#include <string_view>

#include "magmaan/expected.hpp"
#include "magmaan/parse/token.hpp"
#include "magmaan/source_span.hpp"

namespace magmaan::parse {

// Hand-written lexer for the magmaan model syntax. See docs/grammar/lexer.md
// for the full specification. Never throws; failure is returned as a
// ParseError carrying a SourceSpan that points at the offending character.
class Lexer {
 public:
  explicit Lexer(std::string_view src) noexcept;

  // production: TOKEN-STREAM
  //
  // Emits one token per call. End of input returns Token{kind=EndOfFile, ...};
  // calling again is well-defined and keeps returning EndOfFile tokens with
  // an empty span at the end of the source.
  parse_expected<Token> next() noexcept;

  // The position at which the next token would be scanned. Useful in tests
  // and for the parser's "expected X here" error reporting.
  SourceSpan here() const noexcept;

  // True once the lexer has fully consumed its input.
  bool at_eof() const noexcept;

 private:
  std::string_view src_;
  std::uint32_t    pos_;
  std::uint32_t    line_;
  std::uint32_t    col_;
};

}
