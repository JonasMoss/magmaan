// @generated by dev/vendor-cpp.sh from include/magmaan/parse/flat_partable.hpp — DO NOT EDIT; re-run `just vendor`.
#pragma once

#include <cstdint>
#include <memory>
#include <string_view>
#include <variant>
#include <vector>

#include "magmaan/parse/op.hpp"
#include "magmaan/source_span.hpp"

namespace magmaan::parse {

// === Modifier variants ======================================================
// Mirrors `modifier` and `modifier_atom` in docs/grammar/grammar.ebnf.
//
//   ModifierAtom  : a single value (number, label, start value, NA-as-free,
//                   or an equal(...) reference). Carried inside a GroupVec,
//                   one per group.
//   Modifier      : an atom OR a group vector (per-group atoms via c(...)).
//
// StartValue is produced by both the `?` shorthand (`0.5?x`) and the
// start(...) function call (`start(0.5)*x`). The two surfaces collapse to
// the same semantic.
//
// EqualRef is produced by the equal(...) function call (`equal("f=~x2")*x3`):
// it ties this parameter to the one named by `text` (a canonical `lhs op rhs`
// string, or a bare label). lavaanify resolves the reference into a shared
// equality.

struct FixedValue { double value = 0.0; };
struct Label      { std::string_view text; };  // text excludes any surrounding quotes
struct StartValue { double value = 0.0; };
struct Free       { };  // explicit NA modifier (free with no value)
struct EqualRef   { std::string_view text; };  // equal("lhs op rhs") target name

using ModifierAtom = std::variant<FixedValue, Label, StartValue, Free, EqualRef>;

struct GroupVec { std::vector<ModifierAtom> per_group; };

using Modifier =
    std::variant<FixedValue, Label, StartValue, Free, EqualRef, GroupVec>;

// === Flat row ===============================================================
// One row per parsed RHS term. Multi-term formulas (e.g. `f =~ x1 + x2`)
// produce one row per RHS term, all sharing the same `lhs` and `op`.

struct FlatRow {
  std::string_view lhs;
  Op               op       = Op::Measurement;
  std::string_view rhs;          // empty for Op::Intercept
  std::uint32_t    block    = 1; // matches lavaan's lavParseModelString default;
                                 // multi-group blocks land in lavaanify
  std::uint32_t    mod_idx  = 0; // 0 = no modifier; otherwise 1-based index
                                 // into FlatPartable.mods
  SourceSpan       span    = {}; // span covering the originating RHS term

  friend bool operator==(const FlatRow&, const FlatRow&) = default;
};

// === Expression sub-language ================================================
//
// Mirrors the `expr` production in docs/grammar/grammar.ebnf. Built by the
// Pratt parser when a constraint (`==`, `<`, `>`) or define-param (`:=`)
// statement is recognized. Supports identifiers, numeric literals,
// `+ - * / ^`, unary `+ -`, the unary functions `exp` / `log`, and parens.
//
// Closed AST: adding a new variant is an ABI break and requires a major
// version bump. The unary functions reuse `UnNode` (single argument) rather
// than a dedicated function-call node, so `UnOp` spans `Neg/Pos/Exp/Log` —
// adding a `UnOp` enumerator is still source-compatible but every exhaustive
// `switch (UnOp)` must be updated.

struct Expr;
using ExprPtr = std::unique_ptr<Expr>;

struct Num   { double value = 0.0; };
struct Param { std::string_view text; };

enum class BinOp : std::uint8_t { Add, Sub, Mul, Div, Pow };
enum class UnOp  : std::uint8_t { Neg, Pos, Exp, Log };

struct BinNode {
  BinOp   op = BinOp::Add;
  ExprPtr lhs;
  ExprPtr rhs;
};
struct UnNode {
  UnOp    op = UnOp::Neg;
  ExprPtr arg;
};

struct Expr : std::variant<Num, Param, BinNode, UnNode> {
  using variant::variant;
};

// === Constraint =============================================================
//
//   ConstraintKind::Eq        a == b      (lhs and rhs are expressions)
//   ConstraintKind::Lt        a <  b
//   ConstraintKind::Gt        a >  b
//   ConstraintKind::Define    name := expr  (lhs is a single identifier
//                                            stored in `name`; lhs Expr unused)

enum class ConstraintKind : std::uint8_t { Eq, Lt, Gt, Define };

struct Constraint {
  ConstraintKind   kind = ConstraintKind::Eq;
  std::string_view name;     // populated only for Define
  Expr             lhs;      // unused for Define
  Expr             rhs;
  SourceSpan       span;
};

// === Flat partable ==========================================================
// The direct output of the parser. The lavaanify step turns this into a
// full LatentStructure (defaults inserted, free indices assigned, etc.). source_text
// owns the bytes that all string_views in `rows` and `mods` point into.
//
// source_text is std::vector<char> rather than std::string because moving a
// FlatPartable must not invalidate the string_views in rows/mods. SSO would
// relocate a std::string's inline buffer on move; vector<char>'s data
// pointer is heap-allocated and survives moves.

struct FlatPartable {
  std::vector<FlatRow>    rows;
  std::vector<Modifier>   mods;          // 1-indexed; mods[0] is a sentinel
  std::vector<Constraint> constraints;   // ==, <, >, := statements
  std::vector<char>       source_text;   // owned; heap-stable

  FlatPartable() : mods(1) {}            // mods[0] sentinel

  std::string_view source() const noexcept {
    return std::string_view(source_text.data(), source_text.size());
  }

  // Convenience: append a modifier, return its 1-based index for use in
  // FlatRow.mod_idx.
  std::uint32_t add_modifier(Modifier m) {
    mods.push_back(std::move(m));
    return static_cast<std::uint32_t>(mods.size() - 1);
  }
};

}
