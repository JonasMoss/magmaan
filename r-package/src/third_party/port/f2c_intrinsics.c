// @generated by dev/vendor-cpp.sh from third_party/port/f2c_intrinsics.c — DO NOT EDIT; re-run `just vendor`.
/* f2c_intrinsics.c — minimal subset of libf2c's libF77 intrinsics, inlined
 * so we don't need to link the full ~20 KB libF77 archive.
 *
 * PORT (the vendored AMPL/ASL `cport/` plus Fermi-LAT `drmnfb_routines.c`)
 * uses only `pow_dd` and `s_copy` from libF77 — confirmed by `grep -oE
 * "(s_copy|pow_dd|pow_di|d_sign|d_mod|i_dnnt|s_cmp)" third_party/port/...`,
 * which finds exactly these two symbols. Each is a one-screen wrapper around
 * <math.h> / <string.h>.
 *
 * Sources & licensing: the implementations below follow the reference
 * shapes documented at <https://netlib.org/f2c/> (David M. Gay, AT&T /
 * Lucent / Bellcore; permissive license, "permission to use, copy, modify,
 * and distribute this software and its documentation for any purpose and
 * without fee is hereby granted"). Re-implementing the wrappers here is
 * easier than vendoring a libF77 subset because they are tiny and the
 * implementations are mechanically the same across every libf2c packaging.
 */

#include <math.h>
#include "f2c.h"

/* a^b for two doubles. PORT calls `pow_dd(&a, &b)` (Fortran pass-by-reference
 * convention preserved by f2c). */
doublereal pow_dd(doublereal *ap, doublereal *bp)
{
    return pow(*ap, *bp);
}

/* Fortran-style string copy: copies the contents of `b` (length `lb`) into
 * `a` (length `la`). If `lb < la`, pads `a` with blanks. If `lb > la`,
 * truncates `b`. Matches libf2c's `s_copy` exactly; PORT calls it for
 * parameter-name table copies in `dparck_`. */
void s_copy(char *a, char *b, ftnlen la, ftnlen lb)
{
    ftnlen n;

    if (la <= lb) {
        for (n = 0; n < la; ++n) a[n] = b[n];
    } else {
        for (n = 0; n < lb; ++n) a[n] = b[n];
        for (; n < la; ++n)      a[n] = ' ';
    }
}
